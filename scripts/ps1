rename all your images to 1,2,3.jpg...


$i = 1
Get-ChildItem -File | Where-Object {$_.Extension -match "\.(jpg|jpeg|png|gif)$"} | Sort-Object Name | ForEach-Object {
    $newName = "$i$($_.Extension)"
    Rename-Item $_.FullName -NewName $newName
    Write-Host "Renamed $($_.Name) to $newName"
    $i++
}